import React from "react";
import f from './Feeds.module.css';

const Feeds = (props) => {
    return (
        <div className={f.wrapper}>
            Feeds
        </div>
    )
};

export default Feeds;