import React from "react";
import m from './Media.module.css';

const Media = (props) => {
    return (
        <div className={m.wrapper}>
            Media
        </div>
    )
};

export default Media;